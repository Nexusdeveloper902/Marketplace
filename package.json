{
  "name": "digital-marketplace",
  "version": "1.0.0",
  "private": true,
  "description": "Digital Marketplace — marketplace de vehículos de alta gama (HTML + CSS + Vanilla JS + Express + SQLite)",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "db:seed": "node server/database/seed.js",
    "db:reset": "node server/database/seed.js --reset"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "express": "^4.21.2"
  }
}
