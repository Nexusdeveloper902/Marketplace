# Download

This folder is where generated and downloaded files are placed.

It may be empty until a process that produces files (for example, fetching assets or exporting data) writes its output here.
